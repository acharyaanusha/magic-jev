# Vercel AI Gateway key for local runs. On a Vercel deployment OIDC is used instead.
AI_GATEWAY_API_KEY=
# The passphrase the extension sends as x-magic-jev-key. Unset means every request is refused.
ASK_PASSPHRASE=
